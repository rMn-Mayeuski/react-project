import React, { FC } from 'react';
import MainWrapper from '../common/MainWrapper/MainWrapper';
import "./App.scss";

const App: FC = () => {
  return (
    <MainWrapper>
      
    </MainWrapper>
  );
};

export default App;